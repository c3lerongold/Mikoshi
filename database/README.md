# Migrations

O banco é inicializado automaticamente pela API para a primeira execução. A migration Alembic em `versions/0001_initial.py` documenta o esquema inicial e pode ser aplicada com `alembic upgrade head` quando o Alembic estiver configurado.
